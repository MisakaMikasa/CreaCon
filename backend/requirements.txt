fastapi
uvicorn[standard]
jsonschema
pydantic
python-dotenv
Pillow
numpy
scipy

# The desktop window. app.py needs it; main.py (headless) does not.
pywebview

# Pick the SDK(s) for the provider(s) you'll actually use (LLM_PROVIDER in .env)
anthropic
google-genai
